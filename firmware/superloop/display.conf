# Opt-in HMI: west build ... -- -DEXTRA_CONF_FILE=display.conf
# Needs a larger part than the C0116-DK, whose 32 KB flash the font tables overflow.
CONFIG_I2C=y
CONFIG_DISPLAY=y
CONFIG_CHARACTER_FRAMEBUFFER=y
CONFIG_HEAP_MEM_POOL_SIZE=2048
